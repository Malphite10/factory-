# Asset Management

Guidelines for managing static assets (images, icons, videos).

## Optimization
- All images must be optimized for web (WebP/AVIF).
- Icons should be SVG where possible.

## Storage
- Assets for specific projects reside in `projects/{project-id}/assets/`.
- Reusable assets reside in the root `assets/` directory.
