
version: '3.8'

services:
  mistral-agent:
    build: .
    environment:
      - MISTRAL_API_KEY
      - GITHUB_TOKEN
      - FIREWALL_ENABLED=true
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    restart: unless-stopped
